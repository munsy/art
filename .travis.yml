sudo: false

language: go

go:
  - 1.13.x

notifications:
  email: false

install:
  - go get github.com/munsy/art
  - go get -v .
  - go get -v golang.org/x/lint

script:
  - diff <(gofmt -d .) <(echo -n)
  - go vet -x ./...
  - go lint -set_exit_status ./...
  - go test -v -race ./...